import { Meta, Primary, Story, Title } from '@storybook/addon-docs/blocks';

import SingleLineCodeBlock from '../../common/SingleLineCodeBlock';

import * as CardStories from './Card.stories';

<Meta title="Components/Display/Card" of={CardStories} />

<Title>Card</Title>

<div className="max-w-full">
  <SingleLineCodeBlock text={`import { Card } from 'chaya-ui';`} />
</div>

<Primary />

## Usage as Paper

Card can be used a wrapper or container for other components, like a `Paper`, by simply passing the sub-components as
children. This quickly adds some padding, a border, and a subtle background color.

<Story of={CardStories.AsPaper} />

## Nesting Cards

Cards can be nested inside other cards, which would create a "card stack" effect. This is useful for creating a
hierarchical information structure.

<Story of={CardStories.NestedCards} />

This can also be done with custom content, without `title` or `description` props, by simply nesting the cards.

<Story of={CardStories.NestedCardsWithCustomContent} />

## Variants

### Shaded (default)

<Primary />

### Outline

<Story of={CardStories.OutlineVariant} />

## Props

<table>
  <thead>
    <tr>
      <th>Prop</th>
      <th>Type</th>
      <th>Default</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>children</code></td>
      <td><code>ReactNode</code></td>
      <td>-</td>
      <td>Content rendered inside the card body</td>
    </tr>
    <tr>
      <td><code>variant</code></td>
      <td><code>{"'shaded' | 'outline'"}</code></td>
      <td><code>shaded</code></td>
      <td>Visual style variant of the card</td>
    </tr>
    <tr>
      <td><code>title</code></td>
      <td><code>string</code></td>
      <td>-</td>
      <td>Title text displayed in the card header</td>
    </tr>
    <tr>
      <td><code>description</code></td>
      <td><code>string</code></td>
      <td>-</td>
      <td>Description text displayed below the title</td>
    </tr>
    <tr>
      <td><code>titleClassName</code></td>
      <td><code>string</code></td>
      <td>-</td>
      <td>Additional CSS class for the title element</td>
    </tr>
    <tr>
      <td><code>bodyClassName</code></td>
      <td><code>string</code></td>
      <td>-</td>
      <td>Additional CSS class for the card body</td>
    </tr>
    <tr>
      <td><code>titleIcon</code></td>
      <td><code>IconInputType</code></td>
      <td>-</td>
      <td>Icon displayed next to the title</td>
    </tr>
    <tr>
      <td><code>id</code></td>
      <td><code>string</code></td>
      <td>-</td>
      <td>HTML id attribute for the card container</td>
    </tr>
    <tr>
      <td><code>className</code></td>
      <td><code>string</code></td>
      <td>-</td>
      <td>Additional CSS class for the card container</td>
    </tr>
    <tr>
      <td><code>sidebarRenderer</code></td>
      <td><code>ReactNode</code></td>
      <td>-</td>
      <td>Content rendered on the right side of the card header</td>
    </tr>
  </tbody>
</table>
