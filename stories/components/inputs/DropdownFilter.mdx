import { Canvas, Meta, Primary, Title } from '@storybook/addon-docs/blocks';

import SingleLineCodeBlock from '../../common/SingleLineCodeBlock';

import * as DropdownFilterStories from './DropdownFilter.stories';

<Meta title="Components/Inputs/DropdownFilter" of={DropdownFilterStories} />

<Title>DropdownFilter</Title>

<div className="max-w-full">
  <SingleLineCodeBlock text={`import { DropdownFilter } from 'chaya-ui';`} />
</div>

<Primary />

## Async Options

<Canvas of={DropdownFilterStories.Async} withToolbar />

## Props

<table>
  <thead>
    <tr>
      <th>Prop</th>
      <th>Type</th>
      <th>Default</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>children</code></td>
      <td><code>ReactElement</code></td>
      <td>required</td>
      <td>Trigger element that opens the dropdown</td>
    </tr>
    <tr>
      <td><code>selections</code></td>
      <td><code>{"null | any[]"}</code></td>
      <td>required</td>
      <td>Currently selected values</td>
    </tr>
    <tr>
      <td><code>setSelections</code></td>
      <td><code>{"(selections: null | any[]) => void"}</code></td>
      <td>-</td>
      <td>Callback fired when selections change</td>
    </tr>
    <tr>
      <td><code>options</code></td>
      <td><code>{"DropdownFilterOptionType[]"}</code></td>
      <td><code>{"[]"}</code></td>
      <td>Array of options to display</td>
    </tr>
    <tr>
      <td><code>labels</code></td>
      <td><code>{"{ searchPlaceholder?: string; searchLabel?: string; optionsTitle?: string; selectAll?: string; clearAll?: string; noResults?: string }"}</code></td>
      <td>-</td>
      <td>Custom label strings for UI elements</td>
    </tr>
    <tr>
      <td><code>side</code></td>
      <td><code>{"'top' | 'bottom' | 'left' | 'right'"}</code></td>
      <td><code>{"'bottom'"}</code></td>
      <td>Side of the trigger to render the dropdown</td>
    </tr>
    <tr>
      <td><code>align</code></td>
      <td><code>{"'start' | 'end' | 'center'"}</code></td>
      <td><code>{"'start'"}</code></td>
      <td>Alignment of the dropdown relative to the trigger</td>
    </tr>
    <tr>
      <td><code>dropdownContainerClassName</code></td>
      <td><code>string</code></td>
      <td>-</td>
      <td>CSS class for the dropdown container</td>
    </tr>
    <tr>
      <td><code>optionButtonClassName</code></td>
      <td><code>string</code></td>
      <td>-</td>
      <td>CSS class for individual option buttons</td>
    </tr>
    <tr>
      <td><code>isAsync</code></td>
      <td><code>boolean</code></td>
      <td><code>false</code></td>
      <td>Whether to fetch options asynchronously</td>
    </tr>
    <tr>
      <td><code>onFetch</code></td>
      <td><code>{"(keyword: string) => Promise<DropdownFilterOptionType[]>"}</code></td>
      <td>-</td>
      <td>Async function to fetch options based on search keyword</td>
    </tr>
    <tr>
      <td><code>onChangeKeyword</code></td>
      <td><code>{"(keyword: string) => void"}</code></td>
      <td>-</td>
      <td>Callback fired when the search keyword changes</td>
    </tr>
    <tr>
      <td><code>noResultsRenderer</code></td>
      <td><code>{"() => React.ReactNode"}</code></td>
      <td>-</td>
      <td>Custom renderer for the no-results state</td>
    </tr>
  </tbody>
</table>
